<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.gameshare.repository.GameMapper">

    <resultMap id="GameResultMap" type="com.example.gameshare.model.Game">
        <id property="id" column="g_id"/>
        <result property="name" column="g_name"/>
        <result property="description" column="g_description"/>
        <result property="releaseDate" column="g_release_date"/>
        <result property="imageUrl" column="g_image_url"/>
        <result property="categoryId" column="g_category_id"/>
        <result property="createdAt" column="g_created_at"/>
        <result property="updatedAt" column="g_updated_at"/>
        <association property="category" javaType="com.example.gameshare.model.Category">
            <id property="id" column="cat_id"/>
            <result property="name" column="cat_name"/>
            <result property="description" column="cat_description"/>
            <result property="createdAt" column="cat_created_at"/>
            <result property="updatedAt" column="cat_updated_at"/>
        </association>
    </resultMap>

    <sql id="gameColumns">
        g.id as g_id,
        g.name as g_name,
        g.description as g_description,
        g.release_date as g_release_date,
        g.image_url as g_image_url,
        g.category_id as g_category_id,
        g.created_at as g_created_at,
        g.updated_at as g_updated_at
    </sql>

    <sql id="categoryColumnsForGame">
        c.id as cat_id,
        c.name as cat_name,
        c.description as cat_description,
        c.created_at as cat_created_at,
        c.updated_at as cat_updated_at
    </sql>

    <select id="findById" resultMap="GameResultMap">
        SELECT
          <include refid="gameColumns"/>,
          <include refid="categoryColumnsForGame"/>
        FROM games g
        LEFT JOIN categories c ON g.category_id = c.id
        WHERE g.id = #{id}
    </select>

    <select id="findAll" resultMap="GameResultMap">
        SELECT
          <include refid="gameColumns"/>,
          <include refid="categoryColumnsForGame"/>
        FROM games g
        LEFT JOIN categories c ON g.category_id = c.id
        ORDER BY g.id
    </select>

    <select id="findByCategoryId" resultMap="GameResultMap">
        SELECT
          <include refid="gameColumns"/>,
          <include refid="categoryColumnsForGame"/>
        FROM games g
        LEFT JOIN categories c ON g.category_id = c.id
        WHERE g.category_id = #{categoryId}
        ORDER BY g.id
    </select>

    <insert id="insert" parameterType="com.example.gameshare.model.Game" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO games (name, description, release_date, image_url, category_id, created_at, updated_at)
        VALUES (#{name}, #{description}, #{releaseDate, jdbcType=DATE}, #{imageUrl}, #{categoryId}, #{createdAt, jdbcType=TIMESTAMP}, #{updatedAt, jdbcType=TIMESTAMP})
    </insert>

    <update id="update" parameterType="com.example.gameshare.model.Game">
        UPDATE games
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="description != null">description = #{description},</if>
            <if test="releaseDate != null">release_date = #{releaseDate, jdbcType=DATE},</if>
            <if test="imageUrl != null">image_url = #{imageUrl},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt, jdbcType=TIMESTAMP},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM games WHERE id = #{id}
    </delete>

    <select id="search" resultMap="GameResultMap">
        SELECT
          <include refid="gameColumns"/>,
          <include refid="categoryColumnsForGame"/>
        FROM games g
        LEFT JOIN categories c ON g.category_id = c.id
        WHERE g.name LIKE CONCAT('%', #{keyword}, '%')
           OR g.description LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY g.id
    </select>

</mapper>
