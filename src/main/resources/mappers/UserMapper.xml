<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.gameshare.repository.UserMapper">

    <resultMap id="UserResultMap" type="com.example.gameshare.model.User">
        <id property="id" column="u_id"/> <!-- Aliased to avoid conflicts in complex joins -->
        <result property="username" column="u_username"/>
        <result property="password" column="u_password"/>
        <result property="email" column="u_email"/>
        <result property="createdAt" column="u_created_at"/>
        <result property="updatedAt" column="u_updated_at"/>
        <collection property="roles" ofType="com.example.gameshare.model.Role">
            <id property="id" column="r_id"/>
            <result property="name" column="r_name"/>
        </collection>
    </resultMap>

    <!-- A simpler RoleResultMap, assuming RoleMapper.RoleResultMap might not be directly usable here if prefixes change -->
    <resultMap id="UserRoleDetailResultMap" type="com.example.gameshare.model.Role">
        <id property="id" column="r_id"/>
        <result property="name" column="r_name"/>
    </resultMap>

    <sql id="userColumns">
        u.id as u_id,
        u.username as u_username,
        u.password as u_password,
        u.email as u_email,
        u.created_at as u_created_at,
        u.updated_at as u_updated_at
    </sql>

    <sql id="roleColumnsForUser">
        r.id as r_id,
        r.name as r_name
    </sql>

    <select id="findById" resultMap="UserResultMap">
        SELECT
            <include refid="userColumns"/>,
            <include refid="roleColumnsForUser"/>
        FROM users u
        LEFT JOIN user_roles ur ON u.id = ur.user_id
        LEFT JOIN roles r ON ur.role_id = r.id
        WHERE u.id = #{id}
    </select>

    <select id="findByUsername" resultMap="UserResultMap">
        SELECT
            <include refid="userColumns"/>,
            <include refid="roleColumnsForUser"/>
        FROM users u
        LEFT JOIN user_roles ur ON u.id = ur.user_id
        LEFT JOIN roles r ON ur.role_id = r.id
        WHERE u.username = #{username}
    </select>

    <insert id="insert" parameterType="com.example.gameshare.model.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password, email, created_at, updated_at)
        VALUES (#{username}, #{password}, #{email}, #{createdAt, jdbcType=TIMESTAMP}, #{updatedAt, jdbcType=TIMESTAMP})
    </insert>

    <update id="update" parameterType="com.example.gameshare.model.User">
        UPDATE users
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null and password != ''">password = #{password},</if> <!-- Check for non-empty password -->
            <if test="email != null">email = #{email},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt, jdbcType=TIMESTAMP},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM users WHERE id = #{id}
    </delete>

    <select id="findAll" resultMap="UserResultMap">
        SELECT
            <include refid="userColumns"/>,
            <include refid="roleColumnsForUser"/>
        FROM users u
        LEFT JOIN user_roles ur ON u.id = ur.user_id
        LEFT JOIN roles r ON ur.role_id = r.id
        ORDER BY u.id
    </select>

    <insert id="insertUserRole">
        INSERT INTO user_roles (user_id, role_id) VALUES (#{userId}, #{roleId})
    </insert>

    <select id="findRolesByUserId" resultMap="UserRoleDetailResultMap">
        SELECT
            r.id as r_id,
            r.name as r_name
        FROM roles r
        JOIN user_roles ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>

    <delete id="deleteUserRolesByUserId">
        DELETE FROM user_roles WHERE user_id = #{userId}
    </delete>
</mapper>
