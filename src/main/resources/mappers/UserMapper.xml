<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.gameshare.repository.UserMapper">

    <resultMap id="UserResultMap" type="com.example.gameshare.model.User">
        <id property="id" column="id"/>
        <result property="用户名" column="用户名"/>
        <result property="密码" column="密码"/>
        <result property="邮箱" column="邮箱"/>
        <result property="创建时间" column="创建时间"/>
        <result property="更新时间" column="更新时间"/>
        <collection property="角色列表" ofType="com.example.gameshare.model.Role" resultMap="com.example.gameshare.repository.RoleMapper.RoleResultMap" columnPrefix="role_"/>
    </resultMap>

    <select id="findById" resultMap="UserResultMap">
        SELECT
            u.id, u.用户名, u.密码, u.邮箱, u.创建时间, u.更新时间,
            r.id as role_id, r.角色名称 as role_角色名称
        FROM 用户 u
        LEFT JOIN 用户角色 ur ON u.id = ur.用户id
        LEFT JOIN 角色 r ON ur.角色id = r.id
        WHERE u.id = #{id}
    </select>

    <select id="findByUsername" resultMap="UserResultMap">
        SELECT
            u.id, u.用户名, u.密码, u.邮箱, u.创建时间, u.更新时间,
            r.id as role_id, r.角色名称 as role_角色名称
        FROM 用户 u
        LEFT JOIN 用户角色 ur ON u.id = ur.用户id
        LEFT JOIN 角色 r ON ur.角色id = r.id
        WHERE u.用户名 = #{username}
    </select>

    <insert id="insert" parameterType="com.example.gameshare.model.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO 用户 (用户名, 密码, 邮箱, 创建时间, 更新时间)
        VALUES (#{用户名}, #{密码}, #{邮箱}, #{创建时间, jdbcType=TIMESTAMP}, #{更新时间, jdbcType=TIMESTAMP})
    </insert>

    <update id="update" parameterType="com.example.gameshare.model.User">
        UPDATE 用户
        <set>
            <if test="用户名 != null">用户名 = #{用户名},</if>
            <if test="密码 != null">密码 = #{密码},</if>
            <if test="邮箱 != null">邮箱 = #{邮箱},</if>
            <if test="更新时间 != null">更新时间 = #{更新时间, jdbcType=TIMESTAMP},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM 用户 WHERE id = #{id}
    </delete>

    <select id="findAll" resultMap="UserResultMap">
        SELECT
            u.id, u.用户名, u.密码, u.邮箱, u.创建时间, u.更新时间,
            r.id as role_id, r.角色名称 as role_角色名称
        FROM 用户 u
        LEFT JOIN 用户角色 ur ON u.id = ur.用户id
        LEFT JOIN 角色 r ON ur.角色id = r.id
    </select>

    <insert id="insertUserRole">
        INSERT INTO 用户角色 (用户id, 角色id) VALUES (#{userId}, #{roleId})
    </insert>

    <select id="findRolesByUserId" resultMap="com.example.gameshare.repository.RoleMapper.RoleResultMap">
        SELECT r.id, r.角色名称
        FROM 角色 r
        JOIN 用户角色 ur ON r.id = ur.角色id
        WHERE ur.用户id = #{userId}
    </select>

    <delete id="deleteUserRolesByUserId">
        DELETE FROM 用户角色 WHERE 用户id = #{userId}
    </delete>
</mapper>
