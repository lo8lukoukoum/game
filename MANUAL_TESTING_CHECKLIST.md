**特别注意 (Special Note for Testers):** 本项目最近进行了一项重要的重构，将数据库表和列名以及相应的Java对象模型字段从中文更改为英文。虽然应用程序的界面和用户可见数据仍为中文，但此轮测试对于验证后台更改后的数据完整性和各项功能的正确性至关重要。请特别关注：
*   所有涉及数据库交互的功能（如注册、登录、添加/编辑/删除游戏、评论、评分、收藏等）是否按预期工作。
*   中文数据（如用户名、游戏名称、描述、评论内容等）是否在新的英文命名的数据库字段中正确存储和读取。
*   用户角色和权限管理是否依然准确无误。
*   之前所有功能点是否仍然符合预期。

感谢您的仔细测试！

---

# 手动测试清单 (Manual Testing Checklist)

**重要提示:**
*   所有测试点中的用户界面文本（标签、按钮、提示信息等）应为中文。
*   测试前，请确保数据库中已准备好必要的初始数据（例如：管理员用户、游戏分类、游戏条目）。

**I. 用户注册与认证 (User Registration and Authentication)**

1.  **注册 (Registration):**
    *   [ ] 访问注册页面 (`/register`)。
    *   [ ] 尝试使用空字段注册 (预期：看到验证提示信息)。
    *   [ ] 尝试使用已存在的用户名注册 (预期：看到错误提示信息)。
    *   [ ] 尝试使用已存在的邮箱注册 (预期：看到错误提示信息)。
    *   [ ] 成功注册一个新用户。
    *   [ ] 验证用户被重定向到登录页面。
    *   [ ] 验证数据库中为用户分配了默认角色 ('普通用户')。
2.  **登录 (Login):**
    *   [ ] 访问登录页面 (`/login`)。
    *   [ ] 尝试使用空字段登录 (预期：看到验证提示或错误信息)。
    *   [ ] 尝试使用不正确的用户名登录 (预期：看到错误提示 "用户名或密码错误")。
    *   [ ] 尝试使用正确的用户名但不正确的密码登录 (预期：看到错误提示 "用户名或密码错误")。
    *   [ ] 使用正确的凭据成功登录。
    *   [ ] 验证用户被重定向到首页或预期的页面。
    *   [ ] 验证导航栏更新，显示用户专属链接 (我的收藏, 个人资料, 注销)，并隐藏登录/注册链接。
3.  **注销 (Logout):**
    *   [ ] 点击注销链接。
    *   [ ] 验证用户被重定向到登录页面或首页。
    *   [ ] 验证导航栏更新，显示登录/注册链接。
    *   [ ] 尝试访问受保护页面 (例如: `/my-collections`) - 应被重定向到登录页面。
4.  **访问控制 (Access Control - Interceptor):**
    *   [ ] 作为未登录用户，尝试访问 `/my-collections` (预期：重定向到 `/login`)。
    *   [ ] 作为未登录用户，尝试访问 `/admin/games` (预期：重定向到 `/login`)。 (注: 控制器中路径为 `/admin/games-list`)
    *   [ ] 以 '普通用户' 身份登录。
    *   [ ] 尝试访问 `/admin/games` (预期：重定向到 `/access-denied` 或首页)。
    *   [ ] 以 '管理员' 身份登录 (需要手动在数据库中设置或创建管理员账户)。
    *   [ ] 尝试访问 `/admin/games` (预期：成功访问)。

**II. 游戏浏览与详情 (Game Browsing and Details)**

1.  **首页 (`/`):**
    *   [ ] 验证首页加载并显示一些游戏 (如果存在)。
2.  **游戏列表 (`/games`):** (注: 控制器中路径为 `/games-list`)
    *   [ ] 验证页面加载并显示游戏列表 (图片, 名称, 分类)。
    *   [ ] 测试分类筛选功能 (如果已实现并且分类/游戏存在)。
    *   [ ] 测试搜索功能 (按游戏名称/描述搜索)。
        *   [ ] 搜索有结果。
        *   [ ] 搜索无结果。
3.  **游戏详情 (`/games/{id}`):**
    *   [ ] 从列表点击一个游戏。
    *   [ ] 验证游戏详情页显示正确信息 (图片, 名称, 描述, 发行日期, 分类, 平均评分)。
    *   [ ] 验证评论区可见。
    *   [ ] 验证评分区可见。

**III. 用户功能 (User Features - 以 '普通用户' 身份登录)**

1.  **评论 (Comments):**
    *   [ ] 在游戏详情页提交一条新评论。
    *   [ ] 验证评论出现在评论列表中。
    *   [ ] 验证评论已存入数据库。
2.  **评分 (Ratings):**
    *   [ ] 在游戏详情页提交一个新的评分 (例如: 4星)。
    *   [ ] 验证评分被接受，平均评分可能更新。
    *   [ ] 尝试再次评价同一游戏 (应更新现有评分)。
    *   [ ] 验证评分已在数据库中存储/更新。
3.  **收藏 (Collections):**
    *   [ ] 在游戏详情页点击 "添加到收藏"。
    *   [ ] 验证按钮状态改变 (例如: 变为 "从收藏移除" 或给出反馈)。
    *   [ ] 导航到 "我的收藏" 页面 (`/my-collections`)。
    *   [ ] 验证游戏出现在收藏列表中。
    *   [ ] 从 "我的收藏" 或游戏详情页将游戏从收藏中移除。
    *   [ ] 验证游戏从 "我的收藏" 列表中移除。
4.  **个人资料 (`/profile`):**
    *   [ ] 访问个人资料页面。
    *   [ ] 验证基本的户信息已显示。

**IV. 管理员功能 (Admin Features - 以 '管理员' 身份登录)**

1.  **分类管理 (`/admin/categories`):** (注: 控制器中路径为 `/admin/categories-list`)
    *   [ ] 访问分类管理页面。
    *   [ ] 添加一个新的分类 (使用中文名称和描述)。验证其出现在列表中。
    *   [ ] 编辑一个现有分类。验证更改已保存。
    *   [ ] 删除一个分类。验证其已被移除。(需考虑对使用此分类的游戏的影响 - 理想情况下，若仍在使用则阻止删除或提供选项)。
2.  **游戏管理 (`/admin/games`):** (注: 控制器中路径为 `/admin/games-list`)
    *   [ ] 访问游戏管理页面。
    *   [ ] 添加一个新游戏 (使用中文详情, 图片链接, 选择分类)。验证其出现。
    *   [ ] 验证游戏图片在游戏列表和详情页正确显示。
    *   [ ] 编辑一个现有游戏。验证更改。
    *   [ ] 删除一个游戏。验证其已被移除。
3.  **用户管理 (`/admin/users`):** (注: 控制器中路径为 `/admin/users-list`)
    *   [ ] 访问用户管理页面。
    *   [ ] 查看用户列表。
    *   [ ] 编辑一个用户 (例如: 分配/移除 '管理员' 角色)。测试角色更改是否影响登录和访问权限。
    *   [ ] 删除一个用户 (需考虑影响，例如：其评论/评分如何处理 - 软删除或匿名化可能更好)。
4.  **评论管理 (Comment Management - 若有独立管理页面，或通过游戏详情页管理):**
    *   [ ] 作为管理员，查看某游戏的评论。
    *   [ ] 删除一条评论。验证其已被移除。

**V. 通用与界面 (General and UI)**

1.  **响应式设计 (Responsiveness):**
    *   [ ] 在不同屏幕尺寸 (桌面, 平板, 手机) 上检查关键页面 (首页, 游戏列表, 游戏详情)。验证布局调整且可用。
2.  **中文语言 (Chinese Language):**
    *   [ ] 验证所有静态文本 (标签, 按钮, 标题, 消息) 均为中文。
    *   [ ] 验证从数据库获取的数据 (游戏名称, 分类) 能正确显示中文。
3.  **导航 (Navigation):**
    *   [ ] 测试导航栏及页面上的所有链接。
    *   [ ] 测试浏览器的前进/后退按钮。
4.  **错误处理 (Error Handling):**
    *   [ ] 尝试无效操作 (例如: 访问不存在的游戏ID)。预期看到友好的错误页面或消息。
    *   [ ] 测试使用无效数据提交表单，检查服务器端验证。
5.  **数据库时区 (Database Timezone):**
    *   [ ] 验证时间戳 (`创建时间`, `更新时间`) 是否按上海时区预期正确存储和显示 (这可能更多是数据库检查 + 显示检查)。

**初始数据准备 (Initial Data - for testing):**
*   [ ] 手动在数据库中添加至少一个 '管理员' 用户。
*   [ ] 手动在数据库中添加1-2个中文游戏分类。
*   [ ] 手动在数据库中添加2-3个中文游戏条目，包含图片链接并分配到分类。

**注意:** 本清单中对控制器路径的注释 (例如 `/admin/games-list`) 反映了实际开发中使用的路径，可能与最初计划中的路径略有不同。测试时应以实际控制器映射为准。
